{
  "name": "well-known-syndication",
  "version": "0.1.0",
  "private": true,
  "license": "CC0-1.0",
  "type": "module",
  "scripts": {
    "dev": "astro dev",
    "build": "astro build",
    "preview": "astro preview",
    "preview:worker": "astro build && wrangler dev",
    "deploy": "astro build && wrangler deploy"
  },
  "devDependencies": {
    "astro": "^6.0.0",
    "wrangler": "^4.87.0"
  },
  "dependencies": {
    "@fontsource/ibm-plex-mono": "^5.2.7",
    "@fontsource/ibm-plex-sans": "^5.2.8",
    "nanoid": "^5.1.11"
  }
}
